# safelist — allowlist de utilidades de LAYOUT (doc humano)
#
# Fuente real del safelist = @source inline(...) en src/input.css. Este archivo
# es el espejo legible + contrato con el linter (tools/lint.mjs). Mantener en
# sync con input.css. Afinar la palette exacta en Fase 0 (decisión abierta §10.1).
#
# Regla: SOLO layout. Color/tipografía/radio salen de tokens (clases semánticas).
# Arbitrary values (p-[37px], grid-cols-[5]) NUNCA — no se emiten y el linter falla.

# --- display / posición ---
grid  flex  flex-col  flex-1  hidden  relative  absolute
w-full  h-full  max-w-full  mt-auto

# --- grid ---
grid-cols-1  grid-cols-2  grid-cols-3  grid-cols-4
col-span-1  col-span-2  col-span-3  col-span-4

# --- gap (escala de marca: 4=16 6=24 8=32 10=40 12=48 16=64 px) ---
gap-{4,6,8,10,12,16}
gap-x-{4,6,8,10,12,16}
gap-y-{4,6,8,10,12,16}

# --- alineación flex ---
items-{start,center,end,stretch}
justify-{start,center,between,end}

# --- text-align ---
text-left  text-center  text-right

# --- padding / margin (escala de marca) ---
p-{4,6,8,10,12,16}   px-*  py-*  pt-*  pb-*  pl-*  pr-*
m-{4,6,8,10,12,16}   mx-*  my-*  mt-*  mb-*
