# Qué NO se sube a Cloud Build. Explícito a propósito: sin este archivo gcloud deriva uno
# de .gitignore, y cualquier cosa ignorada ahí (hoy node_modules, antes las copias pinneadas
# del CSS) quedaría fuera de la imagen sin aviso.
.git/
.gitignore
.gitattributes
node_modules/
samples/
tests/
*.log
